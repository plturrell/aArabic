# serviceCore Docker Compose File
# First-party services only - All data stored in SAP HANA Cloud
version: '3.8'

# This file includes only serviceCore services
# All third-party infrastructure has been removed
# Data layer: SAP HANA Cloud (via OData) + SAP Object Store

networks:
  servicecore_network:
    name: servicecore_network
    driver: bridge

# Individual service definitions are in docker/compose/*.yml
# Usage:
#   docker-compose -f docker/compose/docker-compose.servicecore.yml up -d

# NOTE: This is a placeholder file. Service definitions have been moved to:
# - docker/compose/docker-compose.servicecore.yml (main services)
